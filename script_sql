=========================
Consulta SQL da Questão 1:
=========================


SELECT
    u.name,
    u.email,
    r.description,
    c.description,
FROM
    users u,
    roles r,
    user_claims uc,
    claims c
WHERE
    u.role_id = r.id
    AND u.id = uc.user_id
    AND uc.claim_id = c.id;
